<h2>Tags:</h2>
<ul>
  <% for (let a of tags) { %>
  <li>
    <a><%=a.name%></a>

    <a href="<%= '/tasks/' + task.taskID + '/removeTag/' + a.tagID %>">❌</a>
  </li>
  <% } %>
</ul>


<%- include("../components/formAddTag.ejs", { task }) %>
